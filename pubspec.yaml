name: dart_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  http: ^1.5.0
  cached_network_image: ^3.2.3
  image_picker: ^1.1.1



flutter:
  uses-material-design: true
  assets:
    - assets/images/

